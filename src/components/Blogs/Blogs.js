import React from 'react';

const Blogs = () => {
    return (
        <div>
            <p>1.One of the best things about React is that we have a lot of different ways to solve specific problems.A context contains references to variables and notifies it's consumers when these references are changed. There are no restrictions on what type of values provide. It can easily pass in functions that can be used to communicate events back from consumers.It is a React API that can solve a lot of problems that modern applications face related to state management and how they're passing state to their components.</p>
            <p>2. Semantic HTML tags allow to add meaning to markup so that search engines, screen readers, and web browsers can make sense of it. By default, when a user agent reads any content it doesn't understand the context and meaning. Semantic HTML tags let they serve structured content to their users, which is especially important for on-page SEO and accessibility. Although semantic tags existed in earlier HTML versions, the HTML5 specifications added several new semantic elements to the syntax—both on the block and inline levels. so, Semantic tags clearly define the purpose of the HTML element. It also describes the type of content that the element should contain.</p>
            
        </div>
    );
};

export default Blogs;